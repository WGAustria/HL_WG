{
  "name": "hl-wg-excel-import",
  "version": "1.0.0",
  "private": true,
  "description": "Upload-Tool: Excel-Datei per LLM auswerten und in eine Excel-Vorlage uebernehmen",
  "scripts": {
    "build:blob-client": "esbuild build/blob-client-entry.js --bundle --platform=browser --format=iife --outfile=blob-client.bundle.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@vercel/blob": "^2.8.0",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
